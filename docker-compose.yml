services:
  solodev:
    build: .
    ports:
      - "3000:3000"
      - "3022:3022"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - solodev-data:/data
    restart: unless-stopped

volumes:
  solodev-data:
