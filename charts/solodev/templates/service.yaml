apiVersion: v1
kind: Service
metadata:
  name: {{ include "solodev.fullname" . }}
  labels:
    {{- include "solodev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "solodev.selectorLabels" . | nindent 4 }}